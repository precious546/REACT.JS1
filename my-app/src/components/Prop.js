import React from 'react'
import Prop2 from './Prop2'

const Prop = (props) => {
  return (
    <div>
        <h1>{props.title}</h1>
        <p>This is a Prop section</p>
    </div>
  )
}

export default Prop;